# --- Servidor ---
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Nota: por ahora el backend usa datos en memoria (sin base de datos todavía).
# Cuando conectemos PostgreSQL, Vertex AI/Gemini y JWT real, aquí se irán
# agregando las variables correspondientes (DB_*, JWT_SECRET, GOOGLE_CLOUD_*).
